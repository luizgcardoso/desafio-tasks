import LayoutForm from "./LayoutForm";

export default function InputTask() {
  return (
    <div className="flex items-center justify-center h-screen">
      <LayoutForm type="Task" />
    </div>
  );
}